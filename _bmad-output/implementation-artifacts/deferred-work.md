# Deferred Work

## Deferred from: code review of 5-3-equal-interval-distribution-cue (2026-07-01)

- `alignPos` uses the dragged swatch's snapped perpendicular coord (`outX`/`outY`), not the chain's actual shared coordinate [lib/swatch-layout.ts:193,196] — flagged independently by both the Blind Hunter and Edge Case Hunter. For a Y-axis (column) cue, `alignPos: outX`; chain membership is `alignedY = others where |o.x − x| ≤ threshold`, so members can share a column X up to `threshold` away from the dragged swatch's snapped X. When the winning X snap is that column member's x (the dominant flow, since `snapHigh` scans other-swatch x's first) `alignPos` is correct; it only diverges when a "column" has members at slightly different x's within threshold, and the error is bounded by `threshold` (~20 canvas px at typical downscale). Purely cosmetic mis-registration of the badge line — never affects the snap position. Every test masks it because the drag coord exactly equals the column/row coord. Fix if the cue ever visibly drifts: derive `alignPos` from the aligned members' shared coord (e.g. their median/first) rather than the dragged swatch's own snapped coord.
- Chain-growth tolerance `tol = threshold/4` scales with on-screen zoom [lib/swatch-layout.ts:62] — `threshold = SNAP_SCREEN_PX / scale`, so on a heavily downscaled stage `tol` grows in canvas px and two gaps differing by several canvas px can merge into one chain (AC3 says stop at the first unequal gap). Not a crash; real chains are near-exact because neighbours were themselves snapped, so this only bites deliberately-near-miss spacing. Tests use fixed `threshold=10` with exact-integer gaps and never exercise the near-miss band. Revisit if over-growth is observed; consider a scale-independent absolute `tol`.
- A neighbour coord exactly equal to `raw` is silently excluded from `lo`/`hi` [lib/swatch-layout.ts:46-47] — the nearest-neighbour scan uses strict `c < raw` / `c > raw`, so a swatch whose coord exactly equals the dragged coord is skipped and `lo`/`hi` jump to the next swatches out. Only reachable when dragging directly on top of another swatch's coord, which drag-end overlap resolution then separates. No test covers coincidence-with-raw. Edge-of-edge; low priority.

## Deferred from: code review of 5-2-cad-style-alignment-snapping-and-guides (2026-06-30, pass 2)

- Konva `onDragMove` position write-back is the known jitter pattern [components/Editor/EyedropperLayer.tsx:142-149] — writing `e.target.x/y()` inside `onDragMove` mutates the node mid-drag without updating Konva's internal drag anchor, the classic cause of snap "jitter." Mirrors the existing `onDragEnd` write-back, and the component test only fires a synthetic `mouseDown`, so it cannot detect real-drag jitter. Whether it actually jitters depends on Konva runtime behaviour — verify with a manual drag before treating as a defect. If it does jitter, the standard fix is to throttle/reconcile against Konva's pointer-derived position rather than overwriting it every frame.
- `handleToggleLabelEdit` uses stale `swatchSide` for free-swatch label offset [components/Editor/index.tsx:504,509] — a freely-placed swatch keeps its pre-detach `swatchSide`, so `getLabelPosition(... p.swatchSide ...)` biases the label offset in the old side's direction. Same class as the Story 5.1 deferred connector-curve stale-side cosmetic bug (below); fold both into one connector-geometry/label-offset cleanup that derives direction from the swatch-vs-marker vector for free swatches. Cosmetic only — nothing breaks.

## Deferred from: code review of 5-2-cad-style-alignment-snapping-and-guides (2026-06-30)

- `placeSwatchOnEdge` / `redistributeOnEdge` orphaned in production [lib/swatch-layout.ts:~139,178] — after `handleSetSide` was removed in Story 5.1's free-floating model, neither function has any production caller; the only references are in `lib/swatch-layout.test.ts`. Free swatches now settle via `resolveSwatchOverlap` instead of edge redistribution, so the entire edge-drop-redistribute path is dead in prod (though still green in tests, giving a false impression it's live). Not introduced by 5.2 — the orphaning originates in 5.1. Either delete both functions + their tests, or add a comment that they're retained for a future edge-drag mode. Per the "mention, don't delete" rule, flagged rather than removed.

## Deferred from: code review of 3-3-label-dragging-and-per-label-controls (2026-06-29)

- "Drag label N" aria-label numbering is non-contiguous and unstable [components/Editor/LabelEditOverlay.tsx:67] — the index `i+1` is taken over the FULL `points` array while hidden / un-laid-out points (`swatchOrder === null`, `!visible`) are filtered out, so visible labels get non-contiguous numbers that shift when an earlier point's visibility/order changes. Consistent with the existing full-array `selectedNumber` convention in `index.tsx:520`, so not a regression; minor a11y polish. Revisit if a stable visible-order numbering is wanted across the panel + overlay.
- Concurrent multi-pointer drags clobber the single shared `dragRef` [components/Editor/LabelEditOverlay.tsx:35-42] — `dragRef` holds exactly one active drag; a second simultaneous pointer (touch) overwrites it, freezing the first label's drag. Effectively unreachable today: the app is desktop-only (mobile-gated at `index.tsx:524`) and mouse is single-pointer. Revisit if touch support lands; per-pointerId drag state would close it.
- ~~Static Konva `LabelLayer` text does not repaint when a webfont finishes loading (FOUT) [components/Editor/LabelLayer.tsx]~~ **ADDRESSED in Story 3.5 (2026-07-01).** A one-shot `document.fonts.ready → layer.batchDraw()` effect in `components/Editor/index.tsx` now repaints canvas labels once all webfonts (incl. the new `Caveat`) have swapped in. No `FontFaceObserver` dependency (as scoped). Original note: `next/font` uses `display: "swap"`; Konva rasterizes text at render time and won't auto-reflow when the `@font-face` swaps in, so display-mode labels kept the fallback font until an unrelated re-render.

## Deferred from: code review of 3-1-style-picker-and-live-style-switching (2026-06-15)

- Switching to a larger-radius style clips a swatch ≤8px past the canvas edge and can overlap neighbours ≤16px until the next drag [components/Editor/index.tsx:391, components/Editor/EyedropperLayer.tsx:32,103] — `handleSelectStyle` only calls `setStyle` and never re-clamps `swatchOrder`, which was bounded under the *previous* radius. `minimal`(r=40)→`float`/`float_clean`/`grid`(r=48): a swatch at `swatchOrder = canvasHeight − 40` renders with r=48 → bottom at `canvasHeight + 8`, clipped by the `<canvas>` bitmap (Konva itself doesn't clip). Adjacent swatches spaced for r=40 (≥80px) overlap when re-drawn at r=48 (need ≥96px) until the next drag re-runs `placeSwatchOnEdge`. Story 3.1 line 33 explicitly pre-authorized NOT adding a re-layout here, so this is a scoped/accepted gap — but note the story's justification ("40 < 48 keeps positions in-bounds") is logically backwards and the dev Completion Note "No out-of-bounds swatch observed" missed the minimal→larger extreme case. Fix when the swatch model is next touched: re-clamp `swatchOrder` to `[r, edge−r]` (or call `assignSwatchLayout`) inside `handleSelectStyle` when `swatchRadius` changes.
- New filled `dot` marker is near-invisible on light areas [styles.json `grid` style, components/Editor/EyedropperLayer.tsx:156] — all 4 styles set `markerColor: "#ffffff"`. The `dot` branch added in 3.1 draws a 6px white filled circle with no stroke; on light regions of a drawing it can disappear (the hollow ring stays more legible against its surroundings). AC-compliant (renders the style's own `markerColor`); the remedy lives in `styles.json` (a darker `markerColor` or a thin contrasting stroke on dots), which was out of scope for 3.1. Revisit in a future style-tuning story.

## Future stories requested during story creation of 3-1-style-picker (2026-06-15)

- **User-uploaded / editable custom styles via UI** — let the artist add or edit styles beyond the 4 built-ins in `styles.json`, through the app rather than by editing the file. Currently a SPEC non-goal (SPEC.md:204 "Uploading custom styles via UI"); the artist asked to revisit it. Own story in a future epic.

## Future stories requested during dev of 3-5-pastel-colored-pencil-swatch-style (2026-07-01)

- **Hand-drawn brush-stroke connector for the pastel style** — the `pastel` style currently reuses the existing crisp `"curved"` Konva `Line` connector. The artist's real annotated study uses a hand-drawn curved-arrow brush stroke (the float example, `~/Downloads/WhatsApp Image 2026-06-11 at 12.50.04.jpeg`, Image 2). Delivering it needs (a) extracting the curved-arrow brush asset to `public/textures/`, and (b) a new textured-connector render path in `EyedropperLayer` (a new optional `connectorTexture` Style field + an image/stroke renderer), analogous to the textured swatch added in 3.5. Explicitly scoped OUT of 3.5 (Dev Notes "Resolved open questions" #1). Sibling/follow-on story under Epic 3.
- ~~**Free-floating, push-aside swatches**~~ **PROMOTED to Epic 5 (2026-06-30).** Became Story 5.1 (free-floating placement) + Story 5.2 (CAD-style snapping & guides) in `epics.md`. DIVERGENCE from this note: on collision the decided behaviour is **block-the-drop** (settle at nearest non-overlapping position, never move neighbours), NOT push-aside — push-aside was explicitly rejected during story grilling. The no-crossing guarantee (CLAUDE.md #3 / SPEC non-negotiable #3) is relaxed to apply only to the generated initial layout. See `5-1-free-floating-swatch-placement.md`.

## Deferred from: code review of 0-1-test-infrastructure (2026-06-14)

- route.ts unguarded `metadata.width!` non-null assertion with no try/catch [app/api/upload/route.ts:27] — a corrupt image (passes the MIME-only client `validateFile` check) makes sharp return undefined dimensions or throw, crashing the route or returning `{width: undefined}`. Pre-existing story-1-2 code; out of scope for the test-infra story.
- Upload component core flow untested (handleUpload fetch success/failure + router.push, handleDrop drag-and-drop, drag-over state) — deferred to story 1-2 (image-upload page & API), whose test task is the natural home for the submit/navigation flow.

## Deferred from: code review of 1-1-project-scaffold-and-core-libraries (2026-06-14)

- canvas-to-916.ts implements vertical-centering logic that story 1-1 Dev Notes explicitly defer to story 1-3 [lib/canvas-to-916.ts:10-12]. Not reverted — the implementation is correct, already tested by story 0-1, and required by story 1-3. To be validated under story 1-3's review, which owns this file.

## Deferred from: code review of 1-2-image-upload-page-and-api (2026-06-14)

- Same-file re-selection doesn't re-fire onChange [components/Upload.tsx:104] — input value never reset to ''; after a failed upload, re-picking the identical file via click does nothing. Low-severity retry-path UX.
- No server-side size cap on file.arrayBuffer() (memory-exhaustion/DoS) and /tmp/<id> dir leaked on failure [app/api/upload/route.ts] — spec scopes server-side validation + /tmp cleanup to story 4.2.
- router.push stuck-uploading state [components/Upload.tsx:49] — if /editor navigation hangs/fails, uploading stays true forever (button disabled). router.push is not a promise, so the catch can't detect it. Low-severity dead-end.

## Deferred from: code review of 1-3-editor-shell-and-9-16-canvas (2026-06-14)

- detectBorderColor calls getImageData once per sampled pixel (~80-160 GPU readbacks) [components/Editor/index.tsx:23-34] — perf smell; batch into a single full-image or edge-strip getImageData and index the flat buffer.
- /api/image uses synchronous fs.existsSync + fs.readFileSync [app/api/image/route.ts] — blocks the Node event loop per request, and a TOCTOU gap between existsSync and readFileSync can throw an uncaught 500 if the 1-hour cleanup removes the file in between. Switch to async fs.promises.readFile with a try/catch. Low-traffic ephemeral tool, low priority.
- /api/image sets Cache-Control: max-age=3600 on files deleted after ~1h [app/api/image/route.ts:18] — browser may serve a stale image after the server file is gone. Low impact (UUIDs never reused). Consider shorter max-age or no-store.

## Deferred from: code review of 2-1-slic-point-suggestion-via-python (2026-06-14)

- Suggested points render imageOffsetY px too high on padded images [components/Editor/EyedropperLayer.tsx:16, Canvas.tsx:42] — SLIC emits `y` in image-pixel space; the image is drawn at `y=imageOffsetY` (Canvas.tsx:42) but circles render at raw `p.y` (EyedropperLayer.tsx:16), so on any portrait image padded to 9:16 every marker sits `imageOffsetY` px too high. Deferred to Story 2.3: spec Dev Notes (lines 343-344) explicitly defer imageOffsetY handling to Story 2.3; markers are temporary placeholders until then. Fix when 2.3 wires up swatch positions — pass `imageOffsetY` into EyedropperLayer and render at `p.y + imageOffsetY`.
- Unbounded stdout/stderr buffering from the spawned python3 process [app/api/suggest/route.ts:30-35] — `stdout += chunk.toString()` has no size cap; a runaway/compromised SLIC script could OOM the Node process. Same memory-exhaustion class as the upload-size-cap item already scoped to story 4.2; address alongside it.
- UUID regex `/^[0-9a-f-]{36}$/` is over-permissive [app/api/suggest/route.ts:9] — accepts non-canonical 36-char strings (e.g. 36 dashes). Path traversal is still blocked by the charset (no `/` or `.`), so not a security hole. Pre-existing pattern, also in app/api/image/route.ts:7; not introduced by this story. Tighten to the anchored 8-4-4-4-12 form across both routes if desired.
- Empty SLIC result renders silently [scripts/slic_suggest.py:51 → components/Editor/index.tsx:120] — a low-contrast/near-uniform image where no segment clears the bg/size filter returns `[]`; the UI clears the spinner and shows no markers and no "no points detected" message, indistinguishable from a silent failure. UX polish; valid success path, no crash.

## Deferred from: code review of 2-2-claude-point-suggestion-optional (2026-06-14)

- Module-level `pointIdCounter` resets on HMR/StrictMode and is shared mutable state [components/Editor/index.tsx:45] — both `apiPointsToEyedroppers` and `claudePointsToEyedroppers` share one monotonic counter; ids are non-deterministic across reloads and reset to 0 on module reload. No current break (nothing keys local state on point id), but a latent footgun once per-point selection/animation state is added (Stories 2.4–2.7). Pre-existing convention from Story 2.1. Consider `crypto.randomUUID()` or a ref-scoped counter when point selection lands.
- No AbortController / sequence guard on `runSuggest`; `setPoints` unconditionally replaces [components/Editor/index.tsx:136-164] — an in-flight suggest for a previous `imageId` is not aborted, so a late-resolving stale response can overwrite a newer one; and any future user edits to points would be silently wiped by a re-run. Low exposure today (imageId is stable from searchParams; no edit features yet). Pre-existing auto-suggest model from Story 2.1; revisit when drag/edit stories (2.4+) make point state worth protecting.
- (Re-confirmed, already tracked above) id regex `/^[0-9a-f-]{36}$/` over-permissive and suggested-point `imageOffsetY` offset — both surfaced again in this review; both remain deferred per the Story 2.1 entries.

## Deferred from: code review of 2-3-eyedropper-points-rendering-and-color-sampling (2026-06-14)

- ~~`markerStyle: "dot"` renders identically to `"ring"` [components/Editor/EyedropperLayer.tsx:95-104]~~ **RESOLVED in Story 3.1 (2026-06-15).** The marker now branches on `style.markerStyle`: `"ring"` → hollow ring (radius 12, no fill, 2px stroke), `"dot"` → filled dot (radius 6, marker-color fill, no stroke), `"none"` → not rendered. Drag/select handlers are shared across ring and dot.

## Deferred from: code review of 2-4-drag-markers-and-live-color-update (2026-06-14)

- null-`swatchOrder` points render nothing, so they have no draggable ring marker on canvas [components/Editor/EyedropperLayer.tsx:63] — `if (p.swatchOrder === null) return null` skips the whole Group. Suggested points are fine (layout runs after `runSuggest`), but the planned Story 2.6 "Add point" tool would create an invisible, undraggable point if it doesn't immediately run `assignSwatchLayout`. Latent trap for 2.6, not a live bug now.
- Ring marker `fill={undefined}` leaves only the 2px stroke annulus hittable [components/Editor/EyedropperLayer.tsx:107] — a 12px-radius ring with no fill registers Konva hits only on the drawn stroke, so clicking the marker's empty center won't start a drag. Pre-existing render style, made relevant now that the marker is draggable. Add `hitStrokeWidth` (or a transparent fill) to widen the grab area; minor UX polish.

## Deferred from: code review of 2-5-drag-swatches-along-edge (2026-06-14)

- `redistributeOnEdge` accepts `swatchRadius` but never uses it [lib/swatch-layout.ts:64,71-73] — even spacing `L*(i+1)/(n+1)` gives no ≥2r overlap guarantee; with default radius 48 on a full-res canvas, ~8+ swatches on one edge visually overlap. Spec-conformant (spec reference code also omits it; mirrors existing `assignSwatchLayout` behavior). Revisit if true minimum-spacing/anti-overlap is wanted — relates to the open decision on whether drop should redistribute-always vs only-on-overlap.
- `Math.max(r, Math.min(edgeLength - r, raw))` inverts when `edgeLength < 2r` [components/Editor/index.tsx:272,293] — for a degenerate sub-96px edge the inner `min` yields `< r` and the outer `max` forces `r`, placing the swatch outside the intended band. Theoretical only — a desktop-only editor with 9:16 full-image canvases never produces edges that short.

## Deferred from: code review of 2-6-add-and-remove-points (2026-06-15)

- Open context menu keeps stale viewport coords after a window resize [components/Editor/index.tsx:346] — `contextMenu.x/y` are frozen `clientX/clientY`; the resize effect (`:128-136`) recenters/rescales the canvas so the right-clicked marker moves, but the open menu stays at its old viewport position. No resize listener repositions or closes it (only outside-mousedown and Escape close). Cosmetic — clicking "Remove point" still removes the correct `pointId`. New to this story; low impact.
- (Re-confirmed, already tracked above) `assignSwatchLayout` has no ≥2r min-spacing/capacity guard [lib/swatch-layout.ts:48-54] — the new Add-point tool makes piling many points onto one edge trivial, so the dense-edge overlap noted in the 2.3/2.5 reviews is now easier to hit. Remains deferred per those entries.
- (Re-confirmed, already tracked above) `runSuggest` blind `setPoints` replace [components/Editor/index.tsx:201] — a point added during the ungated initial auto-suggest is clobbered when the suggest resolves. Remains deferred per the Story 2.2 AbortController/sequence-guard entry.
- (Re-confirmed, already tracked above) Module-global `pointIdCounter` non-deterministic across reloads [components/Editor/index.tsx:51] — remains deferred per the Story 2.2 entry.

## Deferred from: code review of 4-1-jpeg-export-and-download (2026-06-29)

- AC7 capture uses a fixed two-`requestAnimationFrame` wait, not synchronized to Konva draw completion or web-font readiness [components/Editor/index.tsx:513-520] — leaving edit mode then waiting two frames (React commit + one paint) is the story-prescribed recipe, but Konva batch draws aren't tied to RAF count and the italic label font (Cormorant Garamond) may not be loaded on the first paint after the LabelLayer mounts. Rarely, the exported JPEG could show missing/fallback-font labels. **PARTIALLY ADDRESSED in Story 3.5 (2026-07-01):** `handleExport` now `await document.fonts.ready` before `toDataURL`, closing the web-font-readiness half. The Konva-draw-completion half (batch draws not tied to the fixed 2-RAF count) remains — an explicit `layer.batchDraw()` synchronized to draw completion is still the open refinement. Low likelihood, so the remainder stays deferred.
- Very large uploads exceed the browser canvas size cap at 2× pixelRatio [components/Editor/index.tsx:507,520] — `pixelRatio = 2·canvasWidth/displayWidth` targets 2× the original image width, so a very large upload (e.g. 6000px wide → ~12000×21333 export) can exceed the browser's max canvas area; `toDataURL` then returns `""`, the client POSTs `data:,`, the server 400s, and the export silently fails (button just re-enables, no error UI per AC6). Root cause is pre-existing: `/api/upload` does not clamp source dimensions. Fix belongs with an upload-side max-dimension clamp (or a client-side pixelRatio cap + user feedback), out of scope for 4.1.
- `handleExport` has no automated test [components/Editor/index.tsx:498-529] — the two most behavior-dense, highest-risk pieces of the story (the AC4 pixelRatio computation and the AC7 edit-mode toggle/2-frame wait) are verified only by code inspection. The story explicitly sanctions skipping a full EditorShell render (3.3/3.4 precedent), so this is compliant, but AC4 dimensions and AC7 labels remain runtime-unverified. Suggestion: extract the pure pixelRatio math into a `lib/` helper (mirrors the `lib/download.ts` "extract the hard-to-test bit" pattern) so AC4 becomes unit-testable without standing up the shell.

## Deferred from: code review of 5-1-free-floating-swatch-placement (2026-06-30)

- Free swatch connector curve direction uses stale `swatchSide` after detach [components/Editor/EyedropperLayer.tsx:80] — `getCurvedMidpoint` bends the connector based on `p.swatchSide`, but a detached free swatch keeps whatever side it last had (often `"auto"` → no offset). Once dragged across the canvas, the curved connector may bow the wrong way relative to the swatch's new position. Purely cosmetic; nothing breaks. Fits naturally with Story 5.2 (CAD snapping/guides) where connector geometry is revisited.
